{
  "name": "aeon-trespass-expert",
  "private": true,
  "scripts": {
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "build": "pnpm -r run build",
    "codegen:schemas": "node scripts/generate_ts_types.mjs",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.4"
  }
}
